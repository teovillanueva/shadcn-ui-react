{
  "name": "shadcn-ui-react",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "build": "turbo build  --cache-dir=.turbo",
    "dev": "turbo dev",
    "publish-package": "turbo publish-package --cache-dir=.turbo --filter=[HEAD^1] --",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
