{
  "name": "@teovilla/shadcn-ui-react-root",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "publish-packages": "pnpm turbo run publish-package"
  },
  "workspaces": {
    "packages": [
      "packages/generated/*",
      "packages/*",
      "templates/*",
      "scripts/*"
    ]
  },
  "dependencies": {
    "tsup": "^6.7.0"
  },
  "devDependencies": {
    "turbo": "^1.7.0"
  }
}
